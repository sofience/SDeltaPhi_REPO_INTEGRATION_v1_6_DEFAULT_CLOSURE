# Case Pattern — One-Time Exception

A system grants one exception after pressure.

However:

```text
the default remains unchanged
future exceptions cannot re-enter
authority does not change
the route closes again after the case
```

Diagnosis:

```text
one-time exception ≠ persistent reopening
```
