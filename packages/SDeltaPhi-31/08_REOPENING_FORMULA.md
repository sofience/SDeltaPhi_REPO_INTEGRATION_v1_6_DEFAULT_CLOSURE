# Reopening Formula

Let:

```text
D*closed = closed default state
T_r = Trigger Recognition
C_a = Cost Assumption
R_o = Reopening Act
P_r = Persistence of Re-entry
```

Then:

```text
D*closed -> operationally open iff T_r ∧ C_a ∧ R_o ∧ P_r
```

Failure condition:

```text
¬T_r ∨ ¬C_a ∨ ¬R_o ∨ ¬P_r
→ symbolic rupture only
```

Cost asymmetry:

```text
C(P_default) < C(P_reopen)
```
